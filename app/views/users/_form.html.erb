<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <%= form.text_field :username, {placeholder: "username", class: "form-control top-input", required: "", autofocus: ""} %>

    <%= form.password_field :password, {placeholder: "password", class: "form-control middle-input"} %>

    <%= form.password_field :password_confirmation, {placeholder: "confirm password", class: "form-control bottom-input"} %>

    <%= form.submit "Create User", {class: "btn btn-lg btn-primary btn-block"}  %>
<% end %>
